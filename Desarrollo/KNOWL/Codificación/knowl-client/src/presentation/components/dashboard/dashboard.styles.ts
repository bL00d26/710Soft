import { makeStyles } from "@material-ui/core/styles";

export const dashboardStyles = makeStyles((theme) => ({
  //aca van los estilos de layout para el grid que contiene a los compoenntes
  //A
}));
